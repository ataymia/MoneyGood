# Firebase Configuration (for public/firebase.js)
# Get these values from Firebase Console > Project Settings

FIREBASE_API_KEY=your_api_key_here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id

# Cloud Functions Environment Variables (create functions/.env)

STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
APP_URL=https://your-project-id.web.app

# Notes:
# 1. Replace all placeholder values with actual credentials
# 2. For production, use live Stripe keys (sk_live_...)
# 3. Never commit actual credentials to version control
# 4. Update public/firebase.js with Firebase config values
# 5. Create functions/.env with Stripe keys before deploying functions
