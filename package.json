{
  "name": "moneygood",
  "version": "1.0.0",
  "description": "Secure two-party deals with collateral, payments, and dispute resolution",
  "private": true,
  "scripts": {
    "dev": "firebase emulators:start",
    "dev:frontend": "cd public && python -m http.server 8000",
    "build": "cd functions && npm run build",
    "deploy": "npm run build && firebase deploy",
    "deploy:functions": "cd functions && npm run build && cd .. && firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:cloudflare": "echo 'Push to GitHub to trigger Cloudflare Pages deployment'",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes",
    "logs": "firebase functions:log",
    "test:functions": "cd functions && npm test",
    "lint": "cd functions && npm run lint"
  },
  "keywords": [
    "deals",
    "escrow",
    "stripe",
    "firebase",
    "cloudflare"
  ],
  "author": "MoneyGood",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ataymia/MoneyGood.git"
  }
}
